// Package algorithm implements the recommendation pipeline and scoring stages.
//
// The engine consumes store interfaces from the model package and emits
// explainable, deterministic recommendations with optional personalization,
// diversification, and rule enforcement.
package algorithm
