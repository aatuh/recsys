| Capability                               | Status     | Request / Config Details | Notes |
|------------------------------------------|------------|---------------------------|-------|
| Tag include filter                       | Supported  | `POST /v1/recommendations` with `constraints.include_tags_any=['books']` | S1 returned 15/15 book-tagged items; zero leakage (`analysis/evidence/scenario_s1_response.json`). |
| Tag exclude (hard block)                 | Supported  | `POST /v1/admin/rules` (`action=BLOCK`, `target_type=TAG`, `target_key=high_margin`) + `/v1/recommendations` | Blocked tag removed all high_margin items post-rule (`analysis/evidence/scenario_s2_block_high_margin.json`). |
| Manual item boost                        | Supported  | `POST /v1/admin/manual_overrides` (`action=boost`, `boost_value=3.0`) | Target item moved from rank 2 → 0 confirming monotonic boost (`analysis/evidence/scenario_s3_boost.json`). |
| Pin to position                          | Supported  | `POST /v1/admin/rules` (`action=PIN`, `item_ids=['item_0005']`) | Pinned item landed at rank 0 with other slots re-ranked validly (`analysis/evidence/scenario_s5_pin.json`). |
| Brand whitelist                          | Supported  | `POST /v1/recommendations` with `constraints.include_tags_any=['acmetech']` | 10/10 returned items carried the AcmeTech tag (`analysis/evidence/scenario_s6_whitelist.json`). |
| Diversity knob (MMR lambda)              | Supported  | `/v1/recommendations` override `{mmr_lambda:0.1}` | Similarity dropped 0.109→0.070 with NDCG +0.127 (`analysis/evidence/scenario_s4_diversity.json`). |
| Cold-start user defaults                 | Supported  | Upsert new user then `/v1/recommendations` | Cold user received 10 items spanning 8 categories; starter profile trace captured (`analysis/evidence/scenario_s7_cold_start.json`). |
| New-item exposure controls               | Supported  | Manual boost vs baseline on fresh item | Exposure climbed 82%→100% and policy counters reflect boost injection (`analysis/evidence/scenario_s8_new_item.json`). |
| Margin vs relevance trade-off            | Supported  | BOOST rule on `high_margin` tag, `boost_value` ∈ {0.0,0.4,0.8} | Margin share increased 0.386→0.408 with slight NDCG taper (`analysis/evidence/scenario_s9_tradeoff.json`). |
| Explainability & trace                   | Supported  | `/v1/recommendations` with `include_reasons=true`, `explain_level=full` | Responses contain model_version plus reason/explain blocks (`analysis/evidence/scenario_s10_explainability.json`). |
| Deterministic replay (fixed request)     | Supported  | Repeat `POST /v1/recommendations` for `user_0005`, `k=20`, `surface=home` | Five identical ranked lists observed (`analysis/evidence/determinism_check.json`). |
