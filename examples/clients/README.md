# Client Integration Kits

These examples show the minimum production integration shape:

- send tenant/auth headers from a trusted backend,
- preserve request IDs,
- log exposure data when items are shown,
- use pseudonymous identifiers,
- return fallback content when the API fails or returns no items.

The examples are intentionally dependency-light and do not include real credentials.
