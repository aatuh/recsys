{
  "name": "recsys-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4321",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 4321",
    "test:consent": "playwright test --config=playwright.config.ts"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.2",
    "@playwright/test": "^1.57.0",
    "serve": "^14.2.5",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "yaml": "^2.9.0"
  }
}
